# Auto detect text files and perform LF normalization
* text=auto

# Store video files in Git LFS
*.mp4  filter=lfs diff=lfs merge=lfs -text
*.mov  filter=lfs diff=lfs merge=lfs -text
*.webm filter=lfs diff=lfs merge=lfs -text
